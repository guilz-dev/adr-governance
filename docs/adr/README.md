# ADR 運用ルール

- **`docs/adr/`** — accepted / superseded / deprecated（正本）
- **`docs/proposed-adr/`** — proposed / rejected（検討中）

番号は両ディレクトリ通しで一意。昇格時に番号は変えない。

詳細は `.agents/skills/managing-adrs/SKILL.md` を参照。
