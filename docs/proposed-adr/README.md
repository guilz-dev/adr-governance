# Proposed ADRs

Place unresolved architecture decisions here (`status: proposed`). Proposed records are **not** implementation authority until promoted to `docs/adr/` with accepted status and valid acceptance metadata.

Use `adr-governance attest --reviewed-proposal ADR-NNNN` when a change set touches a proposed ADR but the code change is unrelated to that proposal.
